# Frontend
VITE_API_URL=

# Backend

# OpenAI (server-side only; optional while using demo mode)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# OneMap Search API (server-side only; tokens expire after three days)
ONEMAP_ACCESS_TOKEN=

# Supabase (server-side only; use a restricted server key in production)
SUPABASE_URL=
SUPABASE_KEY=
